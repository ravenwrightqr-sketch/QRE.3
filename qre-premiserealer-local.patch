diff --git a/packages/engine/src/experience/premiseRealizer.ts b/packages/engine/src/experience/premiseRealizer.ts
index 177b1c7..898e3d8 100644
--- a/packages/engine/src/experience/premiseRealizer.ts
+++ b/packages/engine/src/experience/premiseRealizer.ts
@@ -789,7 +789,6 @@ function concreteAffordance(
  * than inventing a second mechanic vocabulary or domain-specific story
  * generator.
  */
-
 type ExpressiveSignal =
   | "suspense"
   | "uncertainty"
@@ -799,7 +798,9 @@ type ExpressiveSignal =
   | "escalation"
   | "surprise"
   | "transformation"
-  | "continuation";
+  | "continuation"
+  | "agency";
+
 
 function expressiveSource(
   beat: StoryBeat,
@@ -960,7 +961,29 @@ function expressiveSignals(
   ) {
     signals.push("transformation");
   }
-
+if (
+  hasSignal(source, [
+    /\bagency\b/,
+    /\bchoice\b/,
+    /\bchoose\b/,
+    /\bchooses\b/,
+    /\bchosen\b/,
+    /\bdecide\b/,
+    /\bdecides\b/,
+    /\bdecision\b/,
+    /\bdetermines\b/,
+    /\bdetermined\b/,
+    /\bgets the move\b/,
+    /\bgets to decide\b/,
+    /\bgets to choose\b/,
+    /\bparticipant\b/,
+    /\bparticipants\b/,
+    /\bowner\b/,
+    /\bownership\b/,
+  ])
+) {
+  signals.push("agency");
+}
   if (
     hasSignal(source, [
       /\bcontinu(?:e|ation)\b/,
