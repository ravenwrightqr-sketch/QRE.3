#!/usr/bin/env node
import { existsSync, readFileSync, readdirSync } from "node:fs";
import { join, relative, resolve } from "node:path";
const root=resolve(process.cwd()),fail=[];
const exists=p=>existsSync(join(root,p)),read=p=>readFileSync(join(root,p),"utf8");
const required=["apps/api/src/services/authorBrainCanonical.ts","apps/api/src/services/authorCognition.ts","apps/api/src/services/authorUniversalMovieSearch.ts","apps/api/src/services/authorRealityGraph.ts","apps/api/src/services/authorRealityEnvelope.ts","apps/api/src/services/authorMouth.ts","apps/api/src/services/authorCharacterLensEngine.ts","apps/api/src/services/authorViewerStateCut.ts","apps/api/src/services/authorRealizationMode.ts","apps/api/author-acceptance.ts","apps/api/author-mouth-universal-acceptance.ts"];
const retired=["apps/api/src/services/authorMouthCraft.ts","apps/api/src/services/authorMouthCritic.ts","apps/api/src/services/authorMouthInterpretation.ts","apps/api/src/services/authorMouthSequenceCritic.ts","apps/api/src/services/authorMouthCandidateSearch.ts","apps/api/src/services/authorMouthCandidateSearchCanonical.ts","apps/api/src/services/authorMouthSequenceBeamSearch.ts"];
for(const p of required)if(!exists(p))fail.push(`missing:${p}`);for(const p of retired)if(exists(p))fail.push(`retired Mouth file exists:${p}`);
const brain=exists(required[0])?read(required[0]):"",mouth=exists(required[5])?read(required[5]):"";
for(const [re,label] of [[/buildAuthorCognitivePlan\s*\(/,"brain->cognition"],[/buildAuthorRealityGraph\s*\(/,"brain->reality-graph"],[/buildAuthorRealityEnvelope\s*\(/,"brain->reality-envelope"],[/buildMouthCandidateMessages\s*\(/,"brain->mouth-generation"],[/scoreMouthCandidate\s*\(/,"brain->mouth-scoring"],[/selectBestMouthSequence\s*\(/,"brain->mouth-selection"],[/from\s+["'][^"']*authorViewerStateCut\.js["']/,"brain->viewer-state-cut"],[/from\s+["'][^"']*authorMouth\.js["']/,"brain->canonical-mouth"]])if(!re.test(brain))fail.push(`missing wiring:${label}`);
for(const [re,label] of [[/Reality freedom is LOW\. Framing freedom is HIGH\./,"truth/framing law"],[/Grounding is not authorization\./,"authorization separation"],[/approved-semantic-realization/,"semantic authorization"],[/literal-source-restatement/,"literal fallback"],[/classifyLens/,"lens boundary"]])if(!re.test(mouth))fail.push(`missing Mouth law:${label}`);
const files=[];function walk(dir){if(!existsSync(dir))return;for(const e of readdirSync(dir,{withFileTypes:true})){if(["node_modules",".git","dist","build",".next"].includes(e.name))continue;const a=join(dir,e.name);e.isDirectory()?walk(a):e.isFile()&&/\.(ts|tsx|js|mjs)$/.test(e.name)&&files.push(a)}}walk(join(root,"apps/api/src"));
for(const file of files){const body=readFileSync(file,"utf8"),rel=relative(root,file).replaceAll("\\","/");for(const oldName of ["authorMouthCraft","authorMouthCritic","authorMouthInterpretation","authorMouthSequenceCritic","authorMouthCandidateSearch","authorMouthCandidateSearchCanonical","authorMouthSequenceBeamSearch"])if(new RegExp(`from\\s+[\"'][^\"']*${oldName}\\.js[\"']`).test(body))fail.push(`retired import:${rel}->${oldName}`)}
console.log("=== QRE AUTHOR PRODUCTION GATE ===");for(const f of fail)console.error(`FAIL: ${f}`);if(fail.length){console.error(`AUTHOR PRODUCTION GATE FAILED · ${fail.length} violation(s)`);process.exit(1)}console.log("AUTHOR PRODUCTION GATE GREEN · ONE AUTHOR · ONE COGNITION · ONE MOUTH · ONE SEQUENCE · ZERO MOUTH SEAMS");
