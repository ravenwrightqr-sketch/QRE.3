import fs from "node:fs";
import path from "node:path";

const root = process.cwd();
const files = {
  mouth: path.join(root, "apps/api/src/services/authorMouth.ts"),
  brain: path.join(root, "apps/api/src/services/authorBrainCanonical.ts"),
};

function read(file) {
  if (!fs.existsSync(file)) throw new Error(`Missing file: ${file}`);
  return fs.readFileSync(file, "utf8");
}

function replaceOnce(source, pattern, replacement, label) {
  if (!pattern.test(source)) throw new Error(`Patch anchor not found: ${label}`);
  return source.replace(pattern, replacement);
}

let mouth = read(files.mouth);
let brain = read(files.brain);

mouth = replaceOnce(
  mouth,
  /\/\*\*\n \* Structural explanation detector\.[\s\S]*?\n}\n\nfunction concreteRisk/s,
  `/** Structural detector for explanatory closure, independent of domain facts. */\nfunction explanationRisk(text: string, beat?: MouthCandidateBeat): number {\n  const value = clean(text);\n  if (!value) return 1;\n  if (!beat?.observerExperience?.explanationForbidden) return 0;\n  const explicit = [\n    /\\b(?:because|therefore|thus|hence|due to|as a result|thanks to|all thanks to)\\b/i,\n    /\\b(?:the reason|the cause|the point|the meaning|the secret|the ingredient)\\b/i,\n    /\\b(?:which made|which caused|which meant|that's how|that is how)\\b/i,\n  ];\n  const hits = explicit.reduce((sum, pattern) => sum + (pattern.test(value) ? 1 : 0), 0);\n  return metric(Math.min(1, hits * 0.6));\n}\n\nfunction semanticEventIds(beat: MouthCandidateBeat): string[] {\n  const semantic = beat.semanticRealization;\n  if (!semantic) return [];\n  return uniqueStrings([\n    ...(semantic.evidenceEventIds ?? []),\n    ...(semantic.beforeEventIds ?? []),\n    ...(semantic.afterEventIds ?? []),\n    ...(semantic.callback?.eventIds ?? []),\n  ]);\n}\n\nfunction semanticCarrierFit(text: string, beat: MouthCandidateBeat, envelope: RealityEnvelope): number {\n  const spineIds = semanticEventIds(beat);\n  const beatIds = beat.eventIds ?? [];\n  if (!spineIds.length || !beatIds.some((id) => spineIds.includes(id))) return 0;\n  const candidateWords = meaningful(text);\n  const direct = Math.max(...sourceLabels(beat, envelope).map((label) => overlap(candidateWords, meaningful(label))), 0);\n  const semantic = overlap(candidateWords, meaningful(semanticSource(beat).join(" ")));\n  return metric(Math.max(direct, semantic * 0.9));\n}\n\nfunction groupedEvidenceCoverage(text: string, beat: MouthCandidateBeat, envelope: RealityEnvelope): number {\n  const labels = sourceLabels(beat, envelope);\n  if (labels.length <= 1) return labels.length ? 1 : 0;\n  const candidateWords = meaningful(text);\n  const covered = labels.filter((label) => overlap(candidateWords, meaningful(label)) >= 0.18).length;\n  return metric(Math.max(covered / labels.length, semanticCarrierFit(text, beat, envelope)));\n}\n\nfunction futureEvidenceLeak(text: string, beat: MouthCandidateBeat, envelope: RealityEnvelope): boolean {\n  const positions = (beat.eventIds ?? []).map((id) => envelope.events.findIndex((event) => event.id === id)).filter((position) => position >= 0);\n  if (!positions.length) return false;\n  const latest = Math.max(...positions);\n  const candidateWords = meaningful(text);\n  for (let index = latest + 1; index < envelope.events.length; index += 1) {\n    const event = envelope.events[index];\n    if (event && overlap(candidateWords, meaningful(event.label)) >= 0.55) return true;\n  }\n  return false;\n}\n\nfunction concreteRisk`,
  "Mouth structural discovery helpers",
);

mouth = replaceOnce(
  mouth,
  /function semanticAuthorization\([\s\S]*?\n}\n\nfunction evaluateCandidate/s,
  `function semanticAuthorization(\n  text: string,\n  beat: MouthCandidateBeat,\n  envelope: RealityEnvelope,\n): { authorized: boolean; strength: number } {\n  const semantic = beat.semanticRealization;\n  if (!semantic) return { authorized: false, strength: 0 };\n  const evidence = new Set(semantic.evidenceEventIds ?? []);\n  const eventIds = beat.eventIds ?? [];\n  const candidateWords = meaningful(text);\n  const labels = sourceLabels(beat, envelope);\n  const semanticWords = meaningful(semanticSource(beat).join(" "));\n  const before = meaningful(clean(semantic.before));\n  const after = meaningful(clean(semantic.after));\n  const beforeAfter = before.size > 0 && after.size > 0 && overlap(candidateWords, before) >= 0.18 && overlap(candidateWords, after) >= 0.18;\n  const crossEventExpression = labels.filter((label) => overlap(meaningful(label), candidateWords) >= 0.5).length >= 2;\n  const semanticMove = overlap(candidateWords, semanticWords) >= 0.3;\n  const anchorIds = uniqueStrings([...(semantic.beforeEventIds ?? []), ...(semantic.afterEventIds ?? [])]);\n  const anchorPositions = anchorIds.map((id) => envelope.events.findIndex((event) => event.id === id));\n  const allAnchorsResolve = anchorIds.length >= 2 && anchorPositions.every((position) => position >= 0);\n  const minAnchor = allAnchorsResolve ? Math.min(...anchorPositions) : -1;\n  const maxAnchor = allAnchorsResolve ? Math.max(...anchorPositions) : -1;\n  const bridgeEvent = allAnchorsResolve && minAnchor < maxAnchor && eventIds.some((id) => {\n    const position = envelope.events.findIndex((event) => event.id === id);\n    return position > minAnchor && position < maxAnchor;\n  });\n  const bridgeGrounded = bridgeEvent && labels.some((label) => overlap(candidateWords, meaningful(label)) >= 0.18);\n  const bridgeApproved = bridgeEvent && [...evidence].every((id) => anchorIds.includes(id));\n  const bridge = Boolean(bridgeApproved && bridgeGrounded);\n  const carrier = semanticCarrierFit(text, beat, envelope) >= 0.18;\n  if (!(beforeAfter || crossEventExpression || semanticMove || bridge || carrier)) return { authorized: false, strength: 0 };\n  return {\n    authorized: true,\n    strength: metric((beforeAfter ? 0.38 : 0) + (crossEventExpression ? 0.22 : 0) + (semanticMove ? 0.2 : 0) + (bridge ? 0.12 : 0) + (carrier ? 0.2 : 0)),\n  };\n}\n\nfunction evaluateCandidate`,
  "Mouth semantic authorization",
);

mouth = replaceOnce(
  mouth,
  /function evaluateCandidate\([\s\S]*?\n}\n\nfunction buildSystemPrompt/s,
  `function evaluateCandidate(\n  text: string,\n  beat: MouthCandidateBeat,\n  envelope: RealityEnvelope,\n  priorTexts: readonly string[],\n): MouthCandidate {\n  const value = clean(text);\n  const labels = sourceLabels(beat, envelope);\n  const literal = exactSource(value, labels);\n  const whole = worldSource(envelope).join(" ");\n  const grounding = metric(\n    overlap(meaningful(value), meaningful(labels.join(" "))) * 0.75 +\n      overlap(meaningful(value), meaningful(whole)) * 0.25,\n  );\n  const semantic = semanticAuthorization(value, beat, envelope);\n  const invention = metric(concreteRisk(value, whole));\n  const novelty = priorTexts.length\n    ? metric(1 - Math.max(...priorTexts.map((prior) => overlap(meaningful(value), meaningful(prior))), 0))\n    : 1;\n  const humanSized = wordCount(value) >= 2 && wordCount(value) <= 16;\n  const groupedCoverage = groupedEvidenceCoverage(value, beat, envelope);\n  const explanation = explanationRisk(value, beat);\n  const futureLeak = futureEvidenceLeak(value, beat, envelope);\n  const observerForbidden = beat.observerExperience?.explanationForbidden === true;\n  const semanticEligible = semantic.authorized && !futureLeak && (!observerForbidden || explanation === 0);\n  const discoveryPreserved = semanticEligible && explanation === 0;\n\n  const score = literal\n    ? metric(\n        0.6 +\n          grounding * 0.1 +\n          groupedCoverage * (labels.length > 1 ? 0.08 : 0.02) +\n          novelty * 0.07 +\n          (discoveryPreserved ? 0.04 : 0) -\n          explanation * 0.2,\n      )\n    : metric(\n        semanticEligible\n          ? semantic.strength * 0.34 +\n              groupedCoverage * 0.15 +\n              grounding * 0.08 +\n              novelty * 0.06 +\n              (humanSized ? 0.07 : 0) +\n              0.23 +\n              (discoveryPreserved ? 0.13 : 0) -\n              explanation * 0.48\n          : 0,\n      );\n\n  const reasons: string[] = [];\n  if (literal) reasons.push("literal-source-restatement");\n  if (grounding >= 0.24) reasons.push("event-grounded");\n  if (semanticEligible) reasons.push("approved-semantic-realization");\n  if (semanticEligible && !literal) reasons.push("implied-semantic-realization");\n  else if (!literal && !semanticEligible) reasons.push("candidate-does-not-express-approved-meaning");\n  if ((beat.eventIds ?? []).length > 1 && groupedCoverage >= 0.5) reasons.push("cross-event-expression");\n  if (groupedCoverage >= 0.999) reasons.push("grouped-evidence-complete");\n  if (semanticCarrierFit(value, beat, envelope) >= 0.35) reasons.push("semantic-carrier-realization");\n  if (discoveryPreserved) reasons.push("discovery-preserving");\n  if (explanation > 0) reasons.push("explicit-explanation-risk");\n  if (futureLeak) reasons.push("future-evidence-leak");\n  if (humanSized) reasons.push("human-sized-cut");\n  if (invention >= 0.9) reasons.push("unsupported-concrete-risk");\n\n  return {\n    text: value,\n    beatOrder: beat.order,\n    supportedEventIds:\n      grounding >= 0.18 &&\n      invention < 0.9 &&\n      !futureLeak &&\n      (labels.length <= 1 || groupedCoverage >= 0.5)\n        ? [...(beat.eventIds ?? [])]\n        : [],\n    supportedRelationPairs: (beat.relationKinds ?? []).map(String).filter(Boolean),\n    groundingScore: grounding,\n    meaningScore: semanticEligible ? semantic.strength : literal ? 0.45 : 0,\n    observerDiscoveryScore: semanticEligible\n      ? Math.max(semantic.strength, 0.4) * (explanation === 0 ? 1 : 0.55)\n      : literal\n        ? 0.12\n        : 0,\n    transitionScore: metric(Number(beat.viewerState?.stateShift) || 0.4),\n    obligationCoverage: metric(\n      literal\n        ? labels.length <= 1 ? 1 : 0.5 + groupedCoverage * 0.4\n        : semanticEligible ? 0.72 + groupedCoverage * 0.28 : 0,\n    ),\n    relationContractScore: metric((beat.relationKinds ?? []).length ? 0.85 : 0.35),\n    forbiddenMoveRisk: metric(Math.max(invention, observerForbidden ? explanation : explanation * 0.35)),\n    cohesionScore: metric(\n      0.5 +\n        (semanticEligible ? semantic.strength * 0.3 : 0) +\n        grounding * 0.08 +\n        groupedCoverage * 0.12 -\n        explanation * 0.1,\n    ),\n    noveltyScore: novelty,\n    compressionScore: humanSized ? 0.95 : 0.65,\n    inventionRisk: metric(Math.max(invention, futureLeak ? 0.95 : 0)),\n    repetitionRisk: 1 - novelty,\n    collageRisk: groupedCoverage < 0.5 && labels.length > 1 ? 0.8 : 0,\n    endpointExactness: literal ? 1 : 0,\n    score,\n    reasons,\n  };\n}\n\nfunction buildSystemPrompt`,
  "Mouth candidate evaluation",
);

mouth = replaceOnce(
  mouth,
  /export function isAuthorizedMouthCandidate\([\s\S]*?\n}\n\nfunction lexicalNovelty/s,
  `export function isAuthorizedMouthCandidate(candidate: MouthCandidate): boolean {\n  if (!clean(candidate.text)) return false;\n  if (candidate.inventionRisk >= 0.9 || candidate.forbiddenMoveRisk >= 0.9) return false;\n  if (candidate.reasons.includes("future-evidence-leak")) return false;\n  if (candidate.reasons.includes("explicit-explanation-risk") && candidate.observerDiscoveryScore < 0.3) return false;\n  if (candidate.endpointExactness >= 0.999) return true;\n  return candidate.reasons.includes("approved-semantic-realization");\n}\n\nfunction lexicalNovelty`,
  "Mouth authorization boundary",
);

mouth = replaceOnce(
  mouth,
  /function pathIncrement\([\s\S]*?\n}\n\nfunction dedupe/s,
  `function pathIncrement(\n  candidate: MouthCandidate,\n  prior: readonly MouthCandidate[],\n  pool: MouthCandidatePool,\n): number {\n  const state = pool.viewerState;\n  const novelty = lexicalNovelty(candidate.text, prior);\n  const carry = prior.length\n    ? metric(\n        Math.max(...prior.map((item) => item.meaningScore), 0) * 0.3 +\n          Math.max(...prior.map((item) => item.observerDiscoveryScore), 0) * 0.25 +\n          candidate.meaningScore * 0.45,\n      )\n    : candidate.meaningScore;\n  const fit = metric(\n    candidate.transitionScore * 0.3 +\n      state.stateShift * 0.15 +\n      state.curiosityPressure * 0.2 +\n      state.predictionError * 0.15 +\n      candidate.observerDiscoveryScore * 0.2,\n  );\n  const loop = metric((pool.nextPromise ? 0.6 : 0) + (pool.frontier ? 0.4 : 0));\n  const semanticBonus = candidate.reasons.includes("approved-semantic-realization") ? 0.12 : 0;\n  const discoveryBonus = candidate.reasons.includes("discovery-preserving") ? 0.14 : 0;\n  const carrierBonus = candidate.reasons.includes("semantic-carrier-realization") ? 0.08 : 0;\n  const relationBonus = candidate.reasons.includes("cross-event-expression") ? 0.06 : 0;\n  const explanationPenalty = candidate.reasons.includes("explicit-explanation-risk") ? 0.2 : 0;\n  const penalty = candidate.endpointExactness >= 0.999 && !pool.nextPromise && !pool.frontier ? 0.08 : 0;\n  return metric(\n    candidate.score * 0.27 +\n      fit * 0.19 +\n      carry * 0.14 +\n      candidate.meaningScore * 0.13 +\n      candidate.obligationCoverage * 0.05 +\n      novelty * 0.04 +\n      candidate.cohesionScore * 0.04 +\n      loop * 0.04 +\n      semanticBonus +\n      discoveryBonus +\n      carrierBonus +\n      relationBonus -\n      explanationPenalty -\n      penalty,\n  );\n}\n\nfunction dedupe`,
  "Mouth sequence path scoring",
);

brain = replaceOnce(
  brain,
  /function composeTrajectoryBeats\([\s\S]*?\n}\n\nfunction stepToBeat/s,
  `function composeTrajectoryBeats(\n  movie: LatentMovieCandidate,\n): MouthCandidateBeat[] {\n  if (movie.id === "memory-material") {\n    return unique(\n      movie.trajectory.flatMap((step) => step.eventIds ?? []),\n    ).map((eventId, index, ids) => ({\n      order: index + 1,\n      role: index === ids.length - 1 ? "payoff" : "material",\n      attentionFunction: "Realize approved Living Memory detail with maximum specificity and minimum explanation.",\n      eventIds: [eventId],\n      change: "Make supplied material interesting without inventing an occurrence.",\n      next: "",\n      frontier: "",\n      paysOff: index === ids.length - 1 ? [movie.payoff] : [],\n      relationKinds: unique(movie.supportingRelationKinds),\n      semanticRealization: movie.storyThesis?.semanticRealization,\n      observerExperience: movie.storyThesis?.observerExperience,\n    }));\n  }\n\n  const steps = [...movie.trajectory];\n  if (steps.length <= 1) return steps.map((step) => stepToBeat(movie, step, 0, 1));\n\n  const thesis = movie.storyThesis;\n  const spineIds = unique([\n    ...(thesis?.beforeEventIds ?? []),\n    ...(thesis?.semanticRealization?.evidenceEventIds ?? []),\n    ...(thesis?.afterEventIds ?? []),\n    ...(thesis?.semanticRealization?.callback?.eventIds ?? []),\n    ...(thesis?.sealingEventIds ?? []),\n  ]);\n\n  /*\n   * Semantic spine is protected presentation structure. When Cognition has\n   * actually discovered a before/carrier/after relationship, the carrier and\n   * recontextualizer cannot be swallowed by an opening transformation summary.\n   * For the common 4-step shape this produces: before / carrier+turn / payoff.\n   */\n  if (thesis?.semanticTurn && steps.length >= 4 && spineIds.length >= 2) {\n    const groups: LatentMovieTrajectoryStep[][] = [\n      [steps[0]!],\n      steps.slice(1, -1),\n      [steps[steps.length - 1]!],\n    ].filter((group) => group.length > 0);\n\n    return groups.map((group, groupIndex) => {\n      const final = groupIndex === groups.length - 1;\n      const first = group[0]!;\n      const last = group[group.length - 1]!;\n      const eventIds = unique(group.flatMap((step) => step.eventIds ?? []));\n      const authorityStep: LatentMovieTrajectoryStep = { ...last, eventIds };\n      const canonicalAuthority = realizationAuthorityForBeat(movie, authorityStep);\n      const changes = unique(group.map((step) => step.viewerChange));\n      const change = final\n        ? `Land on the supplied endpoint: ${changes.at(-1) ?? last.viewerChange}`\n        : group.length > 1\n          ? thesis.semanticTurn\n          : changes[0] ?? "Advance through the supplied evidence.";\n\n      return {\n        order: groupIndex + 1,\n        role: final ? "payoff" : groupIndex === 0 ? "establishing" : "reveal",\n        attentionFunction: [\n          clean(first.viewerChange),\n          canonicalAuthority,\n          group.length > 1\n            ? "SEMANTIC SPINE CARRIER CUT: realize the grouped evidence as one perceptual turn; do not enumerate it and do not explain the relationship."\n            : "",\n        ].filter(Boolean).join(" "),\n        creativeMove: group.length > 1 ? "semantic-spine-synthesis" : clean(last.operation) || undefined,\n        eventIds,\n        change,\n        next: clean(last.nextQuestion),\n        frontier: clean(last.nextQuestion),\n        paysOff: final ? [movie.payoff] : [],\n        relationKinds: unique([\n          ...movie.supportingRelationKinds,\n          ...group.flatMap((step) => (step.operation ? [step.operation] : [])),\n        ]),\n        semanticRealization: thesis.semanticRealization,\n        observerExperience: thesis.observerExperience,\n        obligations: [\n          "Preserve all source evidence carried by this cut.",\n          "The semantic spine is an experience structure, not a sentence to repeat.",\n          "Earlier evidence may be accumulated without restating it.",\n          ...(group.length > 1 ? [\n            "The carrier cut must make the supplied relationship perceptible without stating its causal or thematic conclusion.",\n          ] : []),\n          ...(final ? [\n            "The final cut preserves the supplied endpoint and cannot manufacture an additional event.",\n          ] : []),\n        ],\n      };\n    });\n  }\n\n  const groups: LatentMovieTrajectoryStep[][] = [];\n  const total = steps.length;\n  let index = 0;\n  while (index < total) {\n    const group: LatentMovieTrajectoryStep[] = [steps[index]!];\n    const remaining = total - index;\n    const maxGroupSize = remaining >= 8 ? 3 : remaining >= 3 ? 2 : 1;\n    while (group.length < maxGroupSize && index + group.length < total - 1) {\n      const current = group[group.length - 1]!;\n      const next = steps[index + group.length]!;\n      const sameOperation = Boolean(current.operation) && Boolean(next.operation) && current.operation === next.operation;\n      const textSimilarity = overlap(words(clean(current.viewerChange)), words(clean(next.viewerChange)));\n      const contextual = next.operation === "reveal" || next.operation === "establish" || next.operation === "contrast" || next.operation === "recur";\n      const complementary = Boolean(current.eventIds?.length) && Boolean(next.eventIds?.length) && (remaining >= 4 || sameOperation || contextual);\n      const nextIsEndpoint = index + group.length === total - 1;\n      if (nextIsEndpoint || !(sameOperation || textSimilarity >= 0.34 || contextual || complementary)) break;\n      group.push(next);\n    }\n    groups.push(group);\n    index += group.length;\n  }\n\n  if (groups.length < 3 && total >= 4) {\n    groups.length = 0;\n    groups.push([steps[0]!], steps.slice(1, -1), [steps[steps.length - 1]!]);\n  }\n\n  return groups.map((group, groupIndex) => {\n    const final = groupIndex === groups.length - 1;\n    const first = group[0]!;\n    const last = group[group.length - 1]!;\n    const eventIds = unique(group.flatMap((step) => step.eventIds ?? []));\n    const canonicalAuthority = realizationAuthorityForBeat(movie, { ...last, eventIds });\n    const change = synthesizeGroupChange(movie, group, final);\n    return {\n      order: groupIndex + 1,\n      role: final ? "payoff" : groupIndex === 0 ? "establishing" : "reveal",\n      attentionFunction: [clean(first.viewerChange), canonicalAuthority, group.length > 1 ? "This cut is a semantic synthesis of adjacent approved evidence. Realize their joint significance, not a list of source facts." : ""].filter(Boolean).join(" "),\n      creativeMove: group.length > 1 ? "synthesis" : clean(last.operation) || undefined,\n      eventIds,\n      change,\n      next: clean(last.nextQuestion),\n      frontier: clean(last.nextQuestion),\n      paysOff: final ? [movie.payoff] : [],\n      relationKinds: unique([...movie.supportingRelationKinds, ...group.flatMap((step) => (step.operation ? [step.operation] : []))]),\n      semanticRealization: movie.storyThesis?.semanticRealization,\n      observerExperience: movie.storyThesis?.observerExperience,\n      obligations: [\n        "All source event IDs in this cut remain approved evidence.",\n        "Do not turn every source event into a separate sentence.",\n        "Preserve source order while allowing adjacent evidence to share one dramatic function.",\n        ...(group.length > 1 ? ["The realization must express what the grouped evidence means together; do not merely concatenate or enumerate the source details."] : []),\n        ...(final ? ["The final cut must preserve the source-derived endpoint and must not append earlier evidence to the endpoint line."] : []),\n      ],\n    };\n  });\n}\n\nfunction stepToBeat`,
  "Canonical semantic-spine composition",
);

fs.writeFileSync(files.mouth, mouth.replace(/\r\n/g, "\n"), "utf8");
fs.writeFileSync(files.brain, brain.replace(/\r\n/g, "\n"), "utf8");
console.log("AUTHOR SEMANTIC SPINE PATCH GREEN");
console.log("  authorMouth.ts: inference-preserving authorization + carrier protection");
console.log("  authorBrainCanonical.ts: before / carrier-turn / payoff spine preservation");
