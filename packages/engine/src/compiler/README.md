# Compiler Core

`coreCompiler.ts` is the canonical experience compiler entry.

Use:

```ts
import { compileExperience } from "@qre/engine/compiler";
```

Do not add another compiler orchestrator. New intelligence should enrich the existing genome/world/blueprint pipeline or become a projection after the canonical `ExperienceMoment[]` boundary.
