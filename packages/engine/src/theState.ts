import type{TheState,TheStateConfiguration,TheStateCurrent}from"@qre/contracts";export type TheStateAsset={id:string;slug:string;category?:string|null;stateConfig?:TheStateConfiguration|null;experience?:{id:string;title:string|null;createdAt?:string}|null;experiences?:Array<{id:string;title:string|null;createdAt?:string}>};function inWindow(mode:{startsAt?:string|null;endsAt?:string|null;metadata?:Record<string,unknown>},now=Date.now()){const start=mode.startsAt??(typeof mode.metadata?.startsAt==="string"?mode.metadata.startsAt:null);const end=mode.endsAt??(typeof mode.metadata?.endsAt==="string"?mode.metadata.endsAt:null);const s=start?Date.parse(start):NaN;const e=end?Date.parse(end):NaN;if(Number.isFinite(s)&&now<s)return false;if(Number.isFinite(e)&&now>=e)return false;return true;}export function buildTheState(asset:TheStateAsset):TheState{const experiences=Array.isArray(asset.experiences)?asset.experiences:asset.experience?[asset.experience]:[];const config:TheStateConfiguration=asset.stateConfig??{};const modes=config.modes??[];const capabilities=config.capabilities??[];const configuredCurrent:TheStateCurrent=config.current??{modeId:null,status:null,since:null,context:{}};const requested=configuredCurrent.modeId??null;const activeMode=requested?modes.find(m=>m.id===requested&&m.enabled&&inWindow(m)):undefined;return{identity:{id:asset.id,slug:asset.slug,label:asset.experience?.title?.trim()||experiences[0]?.title?.trim()||asset.slug,category:asset.category??undefined},capabilities,modes,current:{...configuredCurrent,modeId:activeMode?.id??null,status:activeMode?(configuredCurrent.status??"active"):"idle"},activeExperienceId:asset.experience?.id??experiences[0]?.id??null,experiences:experiences.map(e=>({id:e.id,title:e.title,createdAt:e.createdAt,available:true})),history:[],measurements:[],patterns:[]};}
