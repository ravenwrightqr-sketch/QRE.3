{
  "name": "qre.3",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm author:guard && pnpm author:wiring && pnpm build:contracts && pnpm build:db && pnpm build:engine && pnpm build:api && pnpm build:web",
    "author:guard": "node ./scripts/verify-contract-ownership.mjs",
    "author:wiring": "node ./scripts/verify-author-wiring.mjs",
    "build:contracts": "pnpm --filter @qre/contracts build",
    "build:db": "pnpm --filter @qre/db build",
    "build:engine": "pnpm --filter @qre/engine build",
    "build:api": "pnpm --filter @qre/api build",
    "build:web": "pnpm --filter @qre/web build",
    "db:migrate": "pnpm --filter @qre/db prisma:deploy",
    "test:memory": "pnpm --filter @qre/engine test:memory",
    "test:elite": "pnpm --filter @qre/engine test:cognition && pnpm --filter @qre/engine test:universal && pnpm --filter @qre/engine test:realization && pnpm --filter @qre/engine test:semantic-realization && pnpm --filter @qre/engine test:super-cog && pnpm --filter @qre/engine test:mechanics && pnpm --filter @qre/engine test:trajectory && pnpm --filter @qre/engine test:concrete-experience && pnpm --filter @qre/engine test:creative && pnpm --filter @qre/engine test:bodyguards && pnpm --filter @qre/engine test:event-realization && pnpm --filter @qre/engine test:memory",
    "clean": "pnpm -r clean"
  },
  "pnpm": {
    "overrides": {
      "@qre/contracts": "workspace:*",
      "@qre/engine": "workspace:*",
      "@qre/db": "workspace:*"
    }
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "20",
    "prisma": "6.19.3",
    "tsx": "^4.22.4",
    "typescript": "5.5.4"
  },
  "dependencies": {
    "@prisma/client": "6.19.3",
    "bcrypt": "^6.19.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "stripe": "^22.2.1",
    "zustand": "^5.0.14"
  }
}
