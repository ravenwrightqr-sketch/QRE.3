# QRE AUTHOR / COGNITION ARCHITECTURE INDEX

**Status:** CANONICAL / CURRENT PRODUCTION ARCHITECTURE

## Master law

```text
ONE REALITY GRAPH
ONE SATANICO INFERENCE AUTHORITY
ONE COGNITION OWNER
ONE MOVIE AUTHORITY
ONE CANONICAL AUTHOR
ONE LENS
ONE MOUTH
ONE MOUTH BEAM
ONE ATTENTION / ARC BOUNDARY
ONE RUNTIME BOUNDARY
ONE LEARNING LOOP
```

No retired implementation, compatibility shim, duplicate semantic authority, or migration-time architecture belongs in production.

## Universal Author

```text
SEE / INPUT
   ↓
SOURCE TRUTH + PROVENANCE
   ↓
REALITY GRAPH
   ↓
SATANICO
   │  relationship constellations
   │  latent interpretations
   │  hypothesis space
   │  observer inference potential
   │  prediction / recontextualization opportunities
   ↓
UNIVERSAL MOVIE SEARCH
   ↓
COGNITION
   ↓
APPROVED MOVIE / BEATS
   ↓
LENS
   ↓
MOUTH
   ↓
MOUTH BEAM
   ↓
ATTENTION / ARC VALIDATION
   ↓
EXPERIENCE
   ↓
SHARE / DISTRIBUTE
   ↓
RETURN
   ↓
NEW REALITY / MEMORY / STATE
```

### Ownership

```text
RealityGraph = supplied reality + explainable derived structure
Satanico = latent relationship and inference discovery
Cognition = movie selection and authorial orchestration
Universal Movie Search = competing experience trajectories
Lens = perception pressure, never fact creation
Mouth = human-facing language realization
Mouth Beam = strongest authorized language path
Attention / Arc = evaluators, not replacement Authors
Runtime = delivery and world-event observation
Learning = governed adaptation from observed outcomes
```

## Satanico objective

Satanico asks:

> What can these real facts make a human reasonably believe without QRE telling them the conclusion?

Primary optimization:

```text
GROUNDING
+ RELATIONAL STRENGTH
+ LATENT INTERPRETABILITY
+ OBSERVER INFERENCE POTENTIAL
+ OBSERVABILITY
+ RECONTEXTUALIZATION
+ PREDICTION MOMENTUM
+ SPECIFICITY
+ UNRESOLVED SPACE
- EXPLANATION
- RESTATEMENT
- OBVIOUSNESS
- PREDICTABILITY
- INVENTION
- UNSUPPORTED CONCLUSION
```

Hard rule:

```text
NEVER RESOLVE AN INFERENCE BEFORE THE OBSERVER GETS THE CHANCE TO MAKE IT.
```

The existing contracts are the vocabulary. `AuthorMetamorphicRelationSet`, `AuthorMetamorphicRelation`, `LatentSemanticRealization`, `ObserverExperienceObjective`, `LatentMovieCandidate`, and `MouthCandidate.observerDiscoveryScore` carry the inference contract through the existing Author path.

## Universal relationship search

Satanico searches the existing RealityGraph for structures such as:

```text
preference constellation
invariant
origin
contrast
callback / recontextualization
accumulation
state transformation
convergence
relational role
```

A domain never receives a special Author. The same graph machinery must handle people, pets, businesses, relationships, objects, properties, places, events, cities, states, travel, memory, and mixed reality.

## Movie law

Movie Search consumes Satanico's competing latent reads before selecting a movie. A fact parade is a fallback, not the preferred movie when grounded inference exists.

A movie optimizes the observer's mental trajectory:

```text
EVIDENCE
 ↓
HYPOTHESIS
 ↓
NEW EVIDENCE
 ↓
PREDICTION UPDATE
 ↓
RECONTEXTUALIZATION
 ↓
OBSERVER COMPLETES THE MEANING
```

## Mouth law

Mouth receives approved reality, approved beats, semantic realization, and observer objective.

Mouth can compress, frame, imply, sharpen, underplay, recontextualize, and create rhythm.

Mouth cannot invent concrete reality or collapse a live inference into an explicit conclusion.

`observerDiscoveryScore` is therefore first-class realization quality.

```text
show the evidence
preserve the pattern
hide the answer
let the observer compute
```

## Product loop

```text
SEE → SCAN → CREATE → SHARE → REPEAT → LOOP
```

The experience is the distribution layer. Every experience is an opportunity to expose the next person, business, place, object, or community to QRE.

## Clean-code law

```text
unused → delete
old → delete
duplicate → delete
temporary → finish or delete
compatibility shim → delete unless required by a genuine external contract
retired implementation → delete
migration script → delete
stale architecture doctrine → delete
```

The current architecture is the only architecture.
