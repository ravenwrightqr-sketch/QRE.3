/* QRE LATENT STORY THESIS */
import type { LatentMovieCandidate, LatentMovieTrajectoryStep, LatentStoryThesis, ObserverExperienceObjective, RealityGraph, RealityRelation } from "@qre/contracts";
import { deriveSequenceBackedCreativeInterpretations, type CreativeInterpretation } from "./authorCreativeInterpretation.js";
const clean=(value:unknown):string=>String(value??"").replace(/\s+/g," ").trim();
const metric=(value:number):number=>Number(Math.max(0,Math.min(1,value)).toFixed(3));
const unique=(values:readonly string[]):string[]=>[...new Set(values.map(clean).filter(Boolean))];
function eventLabel(graph:RealityGraph,id:string):string{return graph.events.find(event=>event.id===id)?.label??"";}
function endpointId(candidate:LatentMovieCandidate):string{const last=candidate.trajectory[candidate.trajectory.length-1];const ids=last?.eventIds??[];return ids[ids.length-1]??"";}
function relationBetween(graph:RealityGraph,from:string,to:string):RealityRelation|undefined{return graph.relations.filter(r=>(r.from===from&&r.to===to)||(r.from===to&&r.to===from)).sort((a,b)=>b.strength-a.strength)[0];}
function relationPriority(kind:RealityRelation["kind"]):number{switch(kind){case "contrasts":return 1;case "recontextualizes":return .95;case "changes":return .85;case "repeats":return .8;case "converges":return .7;case "before":case "after":return .65;case "involves":return .5;default:return .4;}}
type StructuralTurn={step:LatentMovieTrajectoryStep;index:number;relation?:RealityRelation;interpretation?:string};
function meaningfulTurnSteps(candidate:LatentMovieCandidate):Array<{step:LatentMovieTrajectoryStep;index:number}>{return candidate.trajectory.map((step,index)=>({step,index})).filter(({step})=>step.operation!=="establish"&&step.operation!=="payoff"&&step.eventIds.length>=2);}
function interpretationScore(graph:RealityGraph,candidate:LatentMovieCandidate,interpretation:CreativeInterpretation):number{const ids=unique(candidate.trajectory.flatMap(step=>step.eventIds));const index=new Map(ids.map((id,i)=>[id,i]));const supported=interpretation.evidenceEventIds.filter(id=>index.has(id)).length;const coverage=interpretation.evidenceEventIds.length?supported/interpretation.evidenceEventIds.length:0;const positions=interpretation.evidenceEventIds.map(id=>index.get(id)).filter((v):v is number=>v!==undefined);const spread=positions.length>=2&&ids.length>=2?(Math.max(...positions)-Math.min(...positions))/Math.max(1,ids.length-1):0;const endpoint=endpointId(candidate);const endpointSupport=endpoint&&interpretation.evidenceEventIds.includes(endpoint)?1:0;const mechanismPriority:{[K in CreativeInterpretation["mechanism"]]:number}={contrast:1,expectation_shift:.96,convergence:.92,consequence:.9,recurrence:.88,state_change:.84,continuation:.8};return metric(interpretation.confidence*.35+coverage*.2+spread*.18+endpointSupport*.12+mechanismPriority[interpretation.mechanism]*.1);}
function selectCreativeInterpretation(graph:RealityGraph,candidate:LatentMovieCandidate,interpretations:readonly CreativeInterpretation[]):CreativeInterpretation|undefined{return interpretations.map((interpretation,index)=>({interpretation,index,score:interpretationScore(graph,candidate,interpretation)})).sort((a,b)=>b.score-a.score||b.interpretation.confidence-a.interpretation.confidence||b.interpretation.evidenceEventIds.length-a.interpretation.evidenceEventIds.length||a.index-b.index)[0]?.interpretation;}
function strongestStructuralTurn(graph:RealityGraph,candidate:LatentMovieCandidate):StructuralTurn|undefined{const interpretation=selectCreativeInterpretation(graph,candidate,deriveSequenceBackedCreativeInterpretations(graph,candidate));const interpretationScoreValue=interpretation?interpretationScore(graph,candidate,interpretation):0;const explicit=meaningfulTurnSteps(candidate).map(({step,index})=>{const relation=relationBetween(graph,step.eventIds[0]??"",step.eventIds[1]??"");return relation?{step,index,relation,score:relation.strength*.75+relationPriority(relation.kind)*.25}:undefined;}).filter((v):v is {step:LatentMovieTrajectoryStep;index:number;relation:RealityRelation;score:number}=>Boolean(v)).sort((a,b)=>b.score-a.score);const bestExplicit=explicit[0];if(interpretation&&(!bestExplicit||interpretationScoreValue>=bestExplicit.score+.08)){return{step:{order:2,operation:interpretation.mechanism==="contrast"?"contrast":interpretation.mechanism==="recurrence"?"recur":interpretation.mechanism==="convergence"?"converge":interpretation.mechanism==="expectation_shift"?"reframe":interpretation.mechanism==="state_change"?"reveal":"consequence",eventIds:interpretation.evidenceEventIds,viewerChange:interpretation.statement,nextQuestion:"What does this newly meaningful relationship make possible next?"},index:1,interpretation:interpretation.statement};}if(bestExplicit)return bestExplicit;const endpoint=endpointId(candidate);const support=unique(candidate.trajectory.flatMap(step=>step.eventIds)).filter(id=>id!==endpoint);const fallback:Array<{from:string;to:string;relation:RealityRelation}>=[];for(let i=0;i<support.length;i+=1)for(let j=i+1;j<support.length;j+=1){const relation=relationBetween(graph,support[i],support[j]);if(relation)fallback.push({from:support[i],to:support[j],relation});}const best=fallback.sort((a,b)=>(b.relation.strength*.75+relationPriority(b.relation.kind)*.25)-(a.relation.strength*.75+relationPriority(a.relation.kind)*.25))[0];if(!best)return undefined;return{step:{order:2,operation:best.relation.kind==="contrasts"?"contrast":best.relation.kind==="recontextualizes"?"reframe":best.relation.kind==="repeats"?"recur":best.relation.kind==="converges"?"converge":best.relation.kind==="changes"?"reveal":"consequence",eventIds:[best.from,best.to],viewerChange:`${best.relation.kind}: ${eventLabel(graph,best.from)} -> ${eventLabel(graph,best.to)}.`,nextQuestion:"What later supplied evidence makes this change matter?"},index:1,relation:best.relation};}
function buildInitialReading(candidate:LatentMovieCandidate):string{const first=candidate.trajectory.find(step=>step.operation==="establish");return clean(first?.viewerChange||candidate.evidence[0]||"The supplied evidence establishes an initial state.");}
function buildSemanticTurn(graph:RealityGraph,turn:StructuralTurn|undefined):string{if(!turn)return"";if(turn.interpretation)return clean(turn.interpretation);if(!turn.relation)return"";const from=eventLabel(graph,turn.step.eventIds[0]??"");const to=eventLabel(graph,turn.step.eventIds[1]??"");return from&&to?clean(`The reading changes through ${turn.relation.kind}: ${from} -> ${to}.`):"";}
function buildObserverExperienceObjective(graph:RealityGraph,candidate:LatentMovieCandidate,turn:StructuralTurn|undefined):ObserverExperienceObjective|undefined{if(!turn?.interpretation)return undefined;const endpoint=endpointId(candidate);const endpointLabel=endpoint?eventLabel(graph,endpoint):"the supplied ending";const semanticTurn=clean(turn.interpretation);switch(turn.step.operation){case "reframe":return{objective:`Let the observer first accept the supplied reality, then discover that ${semanticTurn.toLowerCase()}`,surprise:"The meaningful change should arrive as a reinterpretation, not as a new event.",curiosity:"What changed even though the world itself did not?",attention:["establish the ordinary","introduce a small discrepancy","withhold the explanation","let supplied evidence recontextualize what came before","land on the supplied endpoint"],landing:`Make ${endpointLabel} feel newly meaningful without explaining why it matters.`,explanationForbidden:true};case "recur":return{objective:`Let the observer notice that repetition has become meaning in ${semanticTurn.toLowerCase()}`,surprise:"The second occurrence should feel more significant than the first without inventing anything new.",curiosity:"Why does this return matter now when it did not before?",attention:["establish the first occurrence","create distance","reintroduce the supplied recurring detail","let recognition arrive","land"],landing:`Let ${endpointLabel} carry the accumulated recognition.`,explanationForbidden:true};case "contrast":return{objective:`Let the observer hold the supplied material in tension until ${semanticTurn.toLowerCase()} becomes felt rather than stated`,surprise:"Reveal the difference between two supplied readings without announcing the contrast.",curiosity:"Which reading is the observer supposed to trust now?",attention:["establish one reading","introduce the conflicting supplied detail","delay resolution","recontextualize","land"],landing:`Let ${endpointLabel} resolve the tension through experience, not explanation.`,explanationForbidden:true};case "converge":return{objective:`Let separate supplied details accumulate until ${semanticTurn.toLowerCase()} becomes obvious in retrospect`,surprise:"The realization should emerge from accumulation rather than a narrated conclusion.",curiosity:"What are these details quietly pointing toward?",attention:["plant independent details","leave their relationship unstated","repeat or deepen the pattern","allow recognition","land"],landing:`Make ${endpointLabel} feel like the natural consequence of what the observer has already noticed.`,explanationForbidden:true};case "reveal":return{objective:`Let the observer experience the shift from supplied action into its newly meaningful state: ${semanticTurn.toLowerCase()}`,surprise:"The state should feel discovered through the action rather than announced afterward.",curiosity:"What did that action change?",attention:["show the supplied action","stay with its immediate consequence","delay naming the state","let the observer recognize the shift","land"],landing:`Let ${endpointLabel} carry the realized state without a summary sentence.`,explanationForbidden:true};case "consequence":return{objective:`Let the observer recognize the consequence of the supplied path: ${semanticTurn.toLowerCase()}`,surprise:"The payoff should feel earned by what came before rather than explained by it.",curiosity:"What did the earlier moment set in motion?",attention:["establish cause","move through supplied consequences","delay interpretation","let the endpoint answer the question","land"],landing:`Make ${endpointLabel} feel earned by the accumulated path.`,explanationForbidden:true};default:return{objective:`Let the observer discover the supplied semantic movement: ${semanticTurn.toLowerCase()}`,surprise:"Prefer a small perceptual discovery over a stated explanation.",curiosity:"What is becoming newly meaningful here?",attention:["establish","accumulate","interrupt expectation","recontextualize","land"],landing:`Let ${endpointLabel} complete the discovery without stating the thesis.`,explanationForbidden:true};}}
function chooseCarrierIds(graph:RealityGraph,turn:StructuralTurn|undefined,candidate:LatentMovieCandidate):string[]{if(!turn)return[];const endpoint=endpointId(candidate);const ids=turn.step.eventIds.filter(id=>id!==endpoint);if(ids.length<=1)return ids;return ids.map(id=>({id,laterParticipation:candidate.trajectory.slice(turn.index+1).filter(step=>step.eventIds.includes(id)).length,relationStrength:graph.relations.filter(r=>r.from===id||r.to===id).reduce((sum,r)=>sum+r.strength,0)})).sort((a,b)=>b.laterParticipation-a.laterParticipation||b.relationStrength-a.relationStrength).slice(0,1).map(item=>item.id);}
function chooseSealingIds(graph:RealityGraph,turn:StructuralTurn|undefined,carriers:readonly string[],candidate:LatentMovieCandidate):string[]{if(!turn)return[];const carrierSet=new Set(carriers);const endpoint=endpointId(candidate);const out:Array<{id:string;score:number}>=[];const push=(id:string,score:number)=>{if(id&&!carrierSet.has(id)&&id!==endpoint&&!out.some(item=>item.id===id))out.push({id,score});};for(let i=turn.index+1;i<candidate.trajectory.length;i+=1){for(const id of candidate.trajectory[i].eventIds){const linked=carriers.some(carrier=>Boolean(relationBetween(graph,carrier,id)));push(id,(candidate.trajectory[i].operation==="payoff"?.2:.7)+(linked?.3:0));}}if(!out.length){const trajectoryIds=new Set(candidate.trajectory.flatMap(step=>step.eventIds));for(const relation of graph.relations){const touches=carriers.some(carrier=>relation.from===carrier||relation.to===carrier);if(!touches)continue;const other=carriers.includes(relation.from)?relation.to:relation.from;if(trajectoryIds.has(other)||other===endpoint)continue;push(other,relation.strength*.75+relationPriority(relation.kind)*.25);}}return out.sort((a,b)=>b.score-a.score).map(item=>item.id).slice(0,2);}
function buildPayoffDependency(graph:RealityGraph,candidate:LatentMovieCandidate,carriers:readonly string[]):string{const endpoint=endpointId(candidate);if(!endpoint)return"";const carrier=carriers[0];if(carrier){const relation=relationBetween(graph,endpoint,carrier);if(relation)return`The supplied endpoint depends on its ${relation.kind} relationship with ${eventLabel(graph,carrier)}.`;}const last=candidate.trajectory.slice(0,-1).flatMap(step=>step.eventIds).find(id=>id!==endpoint);return last?`The supplied endpoint follows the selected trajectory from ${eventLabel(graph,last)}.`:"";}
function counterfactualDependency(graph:RealityGraph,candidate:LatentMovieCandidate,turn:StructuralTurn|undefined,carriers:readonly string[]):number{if(!turn||!carriers.length)return 0;const meaningful=candidate.trajectory.filter(step=>step.operation!=="establish"&&step.operation!=="payoff");const carrier=carriers[0];const later=meaningful.filter(step=>candidate.trajectory.indexOf(step)>turn.index);const dependent=later.filter(step=>step.eventIds.includes(carrier)||step.eventIds.some(id=>Boolean(relationBetween(graph,carrier,id))));const downstream=later.length?dependent.length/later.length:0;const anchored=turn.step.eventIds.includes(carrier)?1:0;const endpoint=endpointId(candidate);const linked=endpoint?Boolean(relationBetween(graph,carrier,endpoint)):false;return metric(downstream*.5+anchored*.3+(linked?.2:0));}
export function deriveLatentStoryThesis(graph:RealityGraph,candidate:LatentMovieCandidate):LatentStoryThesis{const turn=strongestStructuralTurn(graph,candidate);const carrierEventIds=chooseCarrierIds(graph,turn,candidate);const sealingEventIds=chooseSealingIds(graph,turn,carrierEventIds,candidate).filter(id=>!carrierEventIds.includes(id));const turnFromId=turn?.step.eventIds[0]??"";const turnToId=turn?.step.eventIds[1]??"";return{initialReading:buildInitialReading(candidate),semanticTurn:buildSemanticTurn(graph,turn),beforeMeaning:turnFromId?[eventLabel(graph,turnFromId)].filter(Boolean):[],afterMeaning:turnToId?[eventLabel(graph,turnToId)].filter(Boolean):[],beforeEventIds:turnFromId?[turnFromId]:[],afterEventIds:turnToId?[turnToId]:[],relationKind:turn?.relation?.kind,carrierEventIds,sealingEventIds,payoffDependency:buildPayoffDependency(graph,candidate,carrierEventIds),counterfactualDependency:counterfactualDependency(graph,candidate,turn,carrierEventIds),observerExperience:buildObserverExperienceObjective(graph,candidate,turn)};}
