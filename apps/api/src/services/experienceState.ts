import type { ExperienceState, ExperienceTempo, LatentMovieCandidate, RealityGraph, RealityRelation } from "@qre/contracts";
import { detectExperienceContinuity, summarizeExperienceContinuity } from "./experienceContinuity.js";
const clean = (value: unknown): string => String(value ?? "").replace(/\s+/g, " ").trim();
const metric = (value: number): number => Number(Math.max(0, Math.min(1, Number.isFinite(value) ? value : 0)).toFixed(3));
const uniq = <T>(values: readonly T[], limit = 64): T[] => [...new Set(values)].slice(0, limit);
const relationKey = (r: RealityRelation): string => [r.kind,r.from,r.to].join(":");
const relationForStep = (graph: RealityGraph, ids: string[]): RealityRelation | undefined => { if (ids.length < 2) return undefined; const from = ids[0], to = ids[ids.length-1]; return graph.relations.find((r)=>(r.from===from&&r.to===to)||(r.from===to&&r.to===from)); };
const futureWeight = (kind: RealityRelation["kind"]): number => ({causes:1,changes:.95,recontextualizes:.95,contrasts:.95,after:.95,before:.95,converges:.9,repeats:.86,involves:.78,belongs_to:.72}[kind] ?? .65);
function futureIds(graph: RealityGraph, used: readonly string[], payoff: readonly string[]): string[] { const u=new Set(used), p=new Set(payoff); return graph.events.map(e=>{ if(u.has(e.id)||p.has(e.id)) return {id:e.id,score:0}; const score=graph.relations.filter(r=>(r.from===e.id||r.to===e.id)&&(u.has(r.from)||u.has(r.to))).reduce((s,r)=>s+r.strength*futureWeight(r.kind),0); return {id:e.id,score}; }).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,12).map(x=>x.id); }
function deriveTempo(semanticSteps: LatentMovieCandidate["trajectory"], active:number, resolved:number, revisits:number, continuation:number, lookahead:number, endpoint:number, round:number): ExperienceTempo { const tail=semanticSteps.at(-1)?.operation; const urgency=metric(active*.32+lookahead*.28+continuation*.18+Math.min(1,semanticSteps.length/4)*.12+(round>1?.1:0)); if(revisits>0)return{mode:"revisit",urgency,compression:.68,revealSpacing:.58,holdPressure:.52,nextBeatPull:metric(.58+lookahead*.32),reason:"New evidence can change the meaning of established material.",arc:["revisit","reframe",lookahead>.45?"tighten":"hold"]}; if(!semanticSteps.length)return{mode:"hook",urgency:.35,compression:.45,revealSpacing:.75,holdPressure:.25,nextBeatPull:.75,reason:"Establish the world before spending its meaning.",arc:["hook","reveal","open"]}; if(endpoint>=.82||tail==="payoff")return{mode:"release",urgency,compression:.82,revealSpacing:.9,holdPressure:.12,nextBeatPull:metric(.3+continuation*.55),reason:"The selected payoff is close enough to release.",arc:["tighten","release",continuation>.55?"open":"hold"]}; if(active>=2&&lookahead>=.45)return{mode:"tighten",urgency:metric(Math.max(.65,urgency)),compression:.76,revealSpacing:.48,holdPressure:.22,nextBeatPull:metric(.66+lookahead*.25),reason:"Live tensions and a viable next thread demand tighter cuts.",arc:["hook","accelerate","tighten","payoff"]}; if(active>resolved||continuation>=.6)return{mode:"accelerate",urgency:metric(Math.max(.55,urgency)),compression:.7,revealSpacing:.56,holdPressure:.2,nextBeatPull:metric(.6+lookahead*.28),reason:"Unresolved movement is worth carrying forward.",arc:["hook","accelerate","reveal","open"]}; return{mode:"hold",urgency,compression:.52,revealSpacing:.72,holdPressure:.58,nextBeatPull:metric(.48+continuation*.28),reason:"Let the current meaning settle before forcing another turn.",arc:["hook","hold","reframe","release"]}; }
export function buildExperienceState(input:{graph:RealityGraph;movie?:LatentMovieCandidate;lens?:string;memoryContext?:string[];priorExperienceStates?:ExperienceState[];round?:number}):ExperienceState { const {graph,movie}=input; const trajectory=movie?.trajectory??[]; const semanticSteps=trajectory.filter(s=>s.operation!=="establish"&&s.operation!=="payoff"); const payoffStep=trajectory.find(s=>s.operation==="payoff"); const previous=input.priorExperienceStates??[]; const prior=(key:keyof ExperienceState):string[]=>uniq(previous.flatMap(s=>{const v=s[key]; return Array.isArray(v)?v.filter((x):x is string=>typeof x==="string"):[];})); const anchors=uniq([...prior("realityAnchors"),...graph.events.map(e=>clean(e.label)).filter(Boolean)],64); const revisitIds=uniq([...prior("revisitedEventIds"),...detectExperienceContinuity(graph.events,prior("realityAnchors")),...semanticSteps.flatMap(s=>s.eventIds).filter((id,i,ids)=>ids.indexOf(id)!==i)]); const hooks=summarizeExperienceContinuity(graph.events,prior("realityAnchors")); const changed=uniq([...prior("changedEventIds"),...semanticSteps.flatMap(s=>s.eventIds.slice(-1))]); const carrier=uniq([...prior("carrierEventIds"),...semanticSteps.flatMap(s=>s.eventIds)]); const payoffIds=uniq(payoffStep?.eventIds??[]); const relations=semanticSteps.map(s=>relationForStep(graph,s.eventIds)).filter((r):r is RealityRelation=>Boolean(r)); const active=uniq([...prior("activeTensionKeys"),...relations.filter(r=>["contrasts","changes","recontextualizes"].includes(r.kind)).map(relationKey)]); const resolved=uniq([...prior("resolvedTensionKeys"),...relations.filter(r=>["causes","after","converges","repeats"].includes(r.kind)).map(relationKey)]); const unresolved=uniq([...prior("unresolvedQuestions"),...(movie?.unresolvedQuestion?[movie.unresolvedQuestion]:[])],16); const future=futureIds(graph,carrier,payoffIds); const consumed=uniq([...prior("consumedFutureEventIds"),...prior("futureEventIds").filter(id=>carrier.includes(id)||payoffIds.includes(id))],24); const activeFuture=future.filter(id=>!consumed.includes(id)); const retired=uniq([...prior("retiredFutureThreadKeys"),...prior("futureEventIds").filter(id=>consumed.includes(id)).map(id=>`future:${id}`)],32); const lookahead=metric(activeFuture.length?Math.min(1,activeFuture.length/5):0); const continuation=metric(Math.min(1,unresolved.length/4)*.3+Math.min(1,graph.recurringSignals.length/4)*.2+Math.min(1,revisitIds.length/2)*.16+Math.min(1,changed.length/5)*.12+Math.min(1,previous.length/3)*.11+((input.round??1)>1?.15:0)); const endpoint=metric(payoffIds.length?.45+Math.min(.4,changed.length*.05):.1+Math.min(.45,changed.length*.08)); const tempo=deriveTempo(semanticSteps,active.length,resolved.length,revisitIds.length,continuation,lookahead,endpoint,input.round??1); const memoryHooks=uniq([...prior("memoryHooks").filter(h=>!retired.includes(h)),...graph.recurringSignals.map(x=>`recurring:${x}`),...graph.unresolvedTensions.map(x=>`tension:${x}`),...hooks,...revisitIds.map(id=>`revisit:${id}`),...activeFuture.map(id=>`future:${id}`),...retired.slice(-8),`tempo:${tempo.mode}`],48); return {version:1,realityAnchors:anchors,establishedEventIds:uniq([...prior("establishedEventIds"),...trajectory.flatMap(s=>s.eventIds)]),changedEventIds:changed,carrierEventIds:carrier,activeTensionKeys:active,resolvedTensionKeys:resolved,setupEventIds:uniq([...prior("setupEventIds"),...carrier]),callbackEventIds:uniq([...prior("callbackEventIds"),...graph.events.filter(e=>graph.recurringSignals.some(s=>clean(e.label).toLowerCase().includes(clean(s).toLowerCase()))).map(e=>e.id)]),revisitedEventIds:revisitIds,unresolvedQuestions:unresolved,carryThreads:uniq([...prior("carryThreads"),...graph.recurringSignals,...graph.unresolvedTensions,...hooks,...(input.memoryContext??[])],24),futureEventIds:activeFuture,futureThreadKeys:activeFuture.map(id=>`future:${id}`),consumedFutureEventIds:consumed,retiredFutureThreadKeys:retired,semanticTurnKeys:uniq([...prior("semanticTurnKeys"),...relations.map(relationKey)]),relationKinds:uniq([...prior("relationKinds"),...relations.map(r=>r.kind)]),continuationValue:continuation,lookaheadValue:lookahead,endpointPressure:endpoint,attentionPotential:metric(Math.min(1,semanticSteps.length/4)*.25+continuation*.22+lookahead*.2+tempo.nextBeatPull*.18+Math.min(1,revisitIds.length/2)*.15),tempo,selectedLens:clean(input.lens)||"neutral",selectedMovieId:movie?.id,payoffEventIds:payoffIds,earnedByEventIds:uniq([...carrier,...payoffIds]),chapter:{openingEventIds:trajectory[0]?.eventIds??[],finalEventIds:payoffIds,semanticTurns:semanticSteps.map(s=>clean(s.viewerChange)).filter(Boolean),operations:uniq(trajectory.map(s=>s.operation))},memoryHooks}; }
